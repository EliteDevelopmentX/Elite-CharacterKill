Config = {}

-- Allowed groups for using the CK command
Config.AllowedGroups = {
    "admin",
    "owner"
}

-- Framework selection: "esx", "qbcore", or "auto" for automatic detection
Config.Framework = "auto"
